# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for redfish metrics.
    type: object
    properties:
      chassis.powerstate:
        description: "ChassisPowerstateConfig provides config for the chassis.powerstate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "chassis.id"
                - "chassis.asset_tag"
                - "chassis.model"
                - "chassis.name"
                - "chassis.manufacturer"
                - "chassis.serial_number"
                - "chassis.sku"
                - "chassis.chassis_type"
            default:
              - "chassis.id"
              - "chassis.asset_tag"
              - "chassis.model"
              - "chassis.name"
              - "chassis.manufacturer"
              - "chassis.serial_number"
              - "chassis.sku"
              - "chassis.chassis_type"
      chassis.status.health:
        description: "ChassisStatusHealthConfig provides config for the chassis.status.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "chassis.id"
                - "chassis.asset_tag"
                - "chassis.model"
                - "chassis.name"
                - "chassis.manufacturer"
                - "chassis.serial_number"
                - "chassis.sku"
                - "chassis.chassis_type"
            default:
              - "chassis.id"
              - "chassis.asset_tag"
              - "chassis.model"
              - "chassis.name"
              - "chassis.manufacturer"
              - "chassis.serial_number"
              - "chassis.sku"
              - "chassis.chassis_type"
      chassis.status.state:
        description: "ChassisStatusStateConfig provides config for the chassis.status.state metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "chassis.id"
                - "chassis.asset_tag"
                - "chassis.model"
                - "chassis.name"
                - "chassis.manufacturer"
                - "chassis.serial_number"
                - "chassis.sku"
                - "chassis.chassis_type"
            default:
              - "chassis.id"
              - "chassis.asset_tag"
              - "chassis.model"
              - "chassis.name"
              - "chassis.manufacturer"
              - "chassis.serial_number"
              - "chassis.sku"
              - "chassis.chassis_type"
      fan.reading:
        description: "FanReadingConfig provides config for the fan.reading metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "chassis.id"
                - "fan.name"
                - "fan.reading_units"
            default:
              - "chassis.id"
              - "fan.name"
              - "fan.reading_units"
      fan.status.health:
        description: "FanStatusHealthConfig provides config for the fan.status.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      fan.status.state:
        description: "FanStatusStateConfig provides config for the fan.status.state metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      system.powerstate:
        description: "SystemPowerstateConfig provides config for the system.powerstate metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "system.id"
                - "system.asset_tag"
                - "system.bios_version"
                - "system.model"
                - "system.name"
                - "system.manufacturer"
                - "system.serial_number"
                - "system.sku"
                - "system.system_type"
            default:
              - "system.id"
              - "system.asset_tag"
              - "system.bios_version"
              - "system.model"
              - "system.name"
              - "system.manufacturer"
              - "system.serial_number"
              - "system.sku"
              - "system.system_type"
      system.status.health:
        description: "SystemStatusHealthConfig provides config for the system.status.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "system.id"
                - "system.asset_tag"
                - "system.bios_version"
                - "system.model"
                - "system.name"
                - "system.manufacturer"
                - "system.serial_number"
                - "system.sku"
                - "system.system_type"
            default:
              - "system.id"
              - "system.asset_tag"
              - "system.bios_version"
              - "system.model"
              - "system.name"
              - "system.manufacturer"
              - "system.serial_number"
              - "system.sku"
              - "system.system_type"
      system.status.state:
        description: "SystemStatusStateConfig provides config for the system.status.state metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          aggregation_strategy:
            type: string
            enum:
              - "sum"
              - "avg"
              - "min"
              - "max"
            default: "avg"
          attributes:
            type: array
            items:
              type: string
              enum:
                - "system.id"
                - "system.asset_tag"
                - "system.bios_version"
                - "system.model"
                - "system.name"
                - "system.manufacturer"
                - "system.serial_number"
                - "system.sku"
                - "system.system_type"
            default:
              - "system.id"
              - "system.asset_tag"
              - "system.bios_version"
              - "system.model"
              - "system.name"
              - "system.manufacturer"
              - "system.serial_number"
              - "system.sku"
              - "system.system_type"
      temperature.reading:
        description: "TemperatureReadingConfig provides config for the temperature.reading metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      temperature.status.health:
        description: "TemperatureStatusHealthConfig provides config for the temperature.status.health metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      temperature.status.state:
        description: "TemperatureStatusStateConfig provides config for the temperature.status.state metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for redfish resource attributes.
    type: object
    properties:
      host.name:
        description: ResourceAttributeConfig provides common config for a host.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      url.full:
        description: ResourceAttributeConfig provides common config for a url.full resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for redfish metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
